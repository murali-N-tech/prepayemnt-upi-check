# Version notes
# -------------
# The trained model is NOT committed: a scikit-learn pickle is tied to the
# numpy and scikit-learn versions that produced it, so a checked-in one breaks
# on any machine with different versions. Regenerate it after installing:
#
#     python backend/train_model.py
#
# Because of that, these do not need to be exact - they are lower bounds that
# the code is known to work with, plus upper bounds where a major version is
# known to break compatibility. models/metrics.json records the exact versions
# the current model was trained with.

# The floor is 0.115 rather than 0.111 because starlette's TestClient below
# 0.37 passes app= to httpx.Client, which httpx 0.28 removed - the pair
# silently drifts apart and every API test errors at setup. Verified
# against fastapi 0.141 / starlette 1.0 / httpx 0.28.1.
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
python-multipart>=0.0.9
pydantic>=2.7,<3.0

pandas>=2.2,<3.0
numpy>=1.26,<3.0
pyarrow>=16.0
scikit-learn>=1.5           # HistGradientBoostingClassifier(class_weight=) needs >=1.4
joblib>=1.4
shap>=0.45                  # imported at module level by backend/main.py
networkx>=3.2

pdfplumber>=0.11            # primary PDF extractor
pypdf>=4.2                  # legacy fallback
openpyxl>=3.1

bcrypt>=4.1
pyjwt>=2.8

psycopg2-binary>=2.9        # backend/api.py only
streamlit>=1.35             # dashboard/ only
matplotlib>=3.8
seaborn>=0.13

pytest>=8.0
